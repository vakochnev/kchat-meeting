Messenger Bot API предоставляет инструменты для создания ботов и управления ими, 
которые могут взаимодействовать с пользователями посредством сообщений и обработки событий. 
Этот API позволяет разработчикам интегрировать функциональность ботов в свои приложения и платформы.

## Поля ввода: универсальный поиск (type: "search")
## Поля ввода: слайдер (type: "slider")
## Общий диалог отмены и комментариев
## Команда поддержки `/поддержка`

Создаёт обращение в Jira о неработоспособности бота на основе шаблона `task_support_bot_issue.json`.

Шаблон: `config/tasks/templates/task_support_bot_issue.json` — использует макросы:
- `{{workspace_id}}`, `{{group_id}}`, `{{sender_id}}`
- `{{user_email}}`, `{{user_short_name}}`, `{{user_username}}`
- `{{current_date}}`

При успешном создании отображается сообщение об успехе; при ошибке — соответствующее уведомление.


Команда `/отмена` теперь запускает общий диалог:

- Получение списка активных запросов пользователя с backend
- Действия по каждому запросу: отмена, добавление комментария
- Кнопки: `❌ Отменить {id}`, `💬 Комментарий {id}`, а также `❌ Отменить все`, `✅ Готово`
- Текстовый ввод: бот запросит комментарий и применит его к выбранному запросу

Требуемые переменные конфигурации (пример):

```ini
REQUESTS_BASE_URL=https://backend.example.com/api
REQUESTS_LIST_PATH=/requests
REQUESTS_CANCEL_PATH=/requests/{id}/cancel
REQUESTS_COMMENT_PATH=/requests/{id}/comment
REQUESTS_AUTH_TYPE=bearer
REQUESTS_TOKEN=your_token
REQUESTS_TIMEOUT=15
```


Тип `slider` позволяет выбрать числовое значение из диапазона. Поддерживается дробный шаг.

Пример конфигурации:

```json
{
  "name": "weight",
  "type": "slider",
  "label": "Вес",
  "hint": "Выберите значение",
  "minValue": 40,
  "maxValue": 150,
  "step": 1.0,
  "unitName": "кг",
  "ranging": false
}
```

Пояснения:
- minValue/maxValue — границы диапазона
- step — шаг (поддерживает дробные значения)
- unitName — единица измерения (показывается в кнопках)
- ranging — режим выбора диапазона (на данный момент не поддерживается)

Поведение:
- Генерируются кнопки со значениями в указанном диапазоне и шаге, например: `40 кг`, `41 кг`, ...
- Можно ввести значение текстом. Валидация проверит диапазон и кратность шагу. Допускается запись с единицей измерения (`50 кг`) или без (`50`).


Новый универсальный тип заменяет старый `inventory` (удалён из кода).

Пример конфигурации:

```json
{
  "name": "asset_number",
  "type": "search",
  "label": "Инвентарный номер",
  "hint": "Введите инвентарный/учётный номер",
  "table": "equipment",
  "field": "inventory"
}
```

- table — имя таблицы (= `__tablename__` ORM-модели)
- field — имя колонки для точного поиска (`=`)
- label/hint — тексты для пользователя

Поведение:
- При успешном поиске пользователю показывается краткая сводка найденной записи.
- В шаблоне все поля записи доступны через контекст `query`, например `{{query.model}}`.
- При отсутствии результата — шаг не продвигается, запрашивается повторный ввод.

## Документация

- [TOOLS.md](TOOLS.md) — Инструменты мониторинга и администрирования (health check, SSE listener)
- [DATABASE_SETUP.md](DATABASE_SETUP.md) — Настройка базы данных
- [SSE.md](SSE.md) — Архитектура SSE соединений

## Установка

Для установки библиотеки используйте pip:

`` bash
pip устанавливает messenger-bot-api
```

## Application

- **Application**: Основной класс для инициализации вашего бота. Он управляет жизненным циклом и конфигурацией приложения.

### Параметры конструктора

- токен: строка, представляющая токен бота (обязательно).
- request_kwargs: Необязательный словарь дополнительных параметров запроса.

### Методы

- add_handler: Используется для добавления нового обработчика.
- start: запускает бота и начинает выборку и обработку событий.
- stop: Останавливает бота.

### Пример использования

`` python
bot = Application(token, {'api_base_url': rest, 'sse_base_url': sse, 'file_upload_base_url': file})
bot.add_handler(CommandHandler('start', start_handler))
bot.start()
```

### Карта событий

Бот может обрабатывать следующие события:

- MESSAGE
- MESSAGES_DELETED
- MESSAGES_DELETED_COMPLETELY
- MESSAGE_DELETE
- MESSAGE_UPDATE
- GROUP_USER_REMOVE
- GROUP_ME_REMOVE
- GROUP_USER_LEAVE
- GROUP_USERS_ADD
- GROUP_ME_ADD
- GROUP_USER_JOIN
- GROUP_HISTORY_CLEAR
- GROUP_DELETE
- USER_SIGNED_UP_TO_WORKSPACE
- USER_UPDATE

Каждое событие сопоставляется с определенным обработчиком, который соответствующим образом обрабатывает данные события.

## События

- **BotEvent**: Базовый класс для всех событий, связанных с ботом.
- **MessageBotEvent**: Событие, связанное с сообщениями, отправляемыми боту.
- **GroupUserRemoveBotEvent**: Событие, связанное с удалением пользователя из группы.
- **GroupMeRemoveBotEvent**: Событие, связанное с удалением бота из группы.
- **GroupUserLeaveBotEvent**: Событие, которое происходит, когда пользователь покидает группу.
- **GroupUsersAddBotEvent**: Событие, связанное с добавлением пользователей в группу.
- **GroupUserJoinBotEvent**: Событие, которое происходит, когда пользователь присоединяется к группе.
- **GroupMeAddBotEvent**: Событие, связанное с добавлением бота в группу.
- **DeleteGroupBotEvent**: Событие для удаления группы.
- **HistoryClearGroupBotEvent**: Событие для очистки истории группы.
- **UserSignedUpToWorkspaceBotEvent**: Событие, когда пользователь регистрируется в рабочей области.
- **UserUpdateBotEvent**: Событие, когда пользователь обновляется.
- **GroupMeAddBotEvent**: Событие для добавления бота в группу.

### BotEvent

Класс BotEvent предназначен для обработки входящих событий для бота в приложении.
Он предоставляет методы для отправки сообщений, файлов, мультимедиа и т.д.
Класс BotEvent поддерживает различные типы событий:
MESSAGE, 
MESSAGES_DELETED, 
MESSAGES_DELETED_COMPLETELY, 
MESSAGE_DELETE, 
MESSAGE_UPDATE, 
GROUP_USER_REMOVE,
GROUP_ME_REMOVE, 
GROUP_USER_LEAVE, 
GROUP_USERS_ADD, 
GROUP_ME_ADD, 
GROUP_USER_JOIN,
GROUP_HISTORY_CLEAR, 
GROUP_DELETE, 
USER_SIGNED_UP_TO_WORKSPACE, 
USER_UPDATE

### Параметры конструктора:

- **req (Request):**
  Этот параметр является объектом запроса, который содержит информацию и методы, необходимые для выполнения запросов к API бота.

- **sse (sseclient.Event):**
  Этот параметр представляет собой объект event, полученный с помощью механизма отправки событий сервером (SSE).
  Он содержит данные о конкретном событии, произошедшем в чате или группе.
  sseclient.Event объект содержит такие атрибуты, как:
    - event: тип события.
    - id: уникальный идентификатор события.
    - data: полезная нагрузка события.

#### Методы:

1. **send_text_message**. Этот метод предназначен для отправки текстового сообщения определенной группе и рабочей области.

   **_параметры_**:
    - **workspace_id (int):**
      Уникальный идентификатор рабочей области, в которой находится группа.
    - **group_id (int):**
      Уникальный идентификатор группы, которой будет отправлено сообщение.
    - **message (MessageRequest):**
      Объект сообщения, содержащий текст, идентификатор ответа, кнопки, параметры форматирования

2. **send_text**. Этот метод предназначен для отправки текста определенной группе и рабочей области.

   **_параметры_**:
    - **workspace_id (int):**
      Уникальный идентификатор рабочей области, в которой находится группа.
    - **group_id (int):**
      Уникальный идентификатор группы, которой будет отправлено сообщение.
    - **text (str):**
      Текст сообщения

3. **send_file**. Этот метод предназначен для отправки файла и текста в определенную группу и рабочее пространство.

   **_параметры_**:
    - **workspace_id (int):**
      Уникальный идентификатор рабочей области, в которой находится группа.
    - **group_id (int):**
      Уникальный идентификатор группы, которой будет отправлено сообщение.
    - **file_path (str):**
      Путь к файлу на диске.
    - **text (str):**
      Текст сообщения

4. **send_file_message**. Этот метод предназначен для отправки файла и сообщения определенной группе и рабочему пространству.

   **_Parameters_**:
    - **workspace_id (int):**
      Уникальный идентификатор рабочего пространства, в котором находится группа.
    - **group_id (int):**
      Уникальный идентификатор группы, которой будет отправлено сообщение.
    - **file (File):**
      Файл, содержит имя файла и содержимое в байтах.
    - **message (MessageRequest):**
      Объект сообщения, содержащий текст, идентификатор ответа, кнопки, параметры форматирования

5. **send_multimedia**.Этот метод предназначен для отправки мультимедиа и текста определенной группе и рабочей области.

   **_параметры_**:
    - **workspace_id (int):**
      Уникальный идентификатор рабочей области, в которой находится группа.
    - **group_id (int):**
      Уникальный идентификатор группы, которой будет отправлено сообщение.
    - **images (необязательно[Список[изображений]]):**
      Список изображений, изображение содержит имя файла и содержимое в байтах
    - **videos (необязательно[Список[видео]]):**
      Список видеороликов, видео содержит имя файла и содержимое в байтах
    - **text (str):**
      Текст сообщения

6. **send_multimedia_message**.Этот метод предназначен для отправки мультимедийных сообщений определенной группе в пределах заданного
   рабочее место.

   **_Parameters_**:
    - **workspace_id (int):**
      Уникальный идентификатор рабочей области, в которой находится группа.
    - **group_id (int):**
      Уникальный идентификатор группы, которой будет отправлено сообщение.
    - **изображения (необязательно [Список[Изображение]]):**
      Список изображений, изображение содержит имя файла и содержимое в байтах
    - **videos (необязательно[Список[Видео]]):**
      Список видео, видео содержит имя файла и содержимое в байтах
    - **message (MessageRequest):**
      Объект сообщения, содержащий текст, идентификатор ответа, кнопки, настройки форматирования

7. **delete_messages**. Этот метод предназначен для удаления сообщений определенной группы в пределах заданного
   рабочее место.

   **_параметры_**:
    - **workspace_id (int):**
      Уникальный идентификатор рабочей области, в которой находится группа.
    - **group_id (int):**
      Уникальный идентификатор группы, которой будет отправлено сообщение.
    - **message_ids (List[int]):**
      Уникальные идентификаторы сообщений

8. **confirm_event**. Этот метод предназначен для подтверждения события для определенной группы в рамках данного
   рабочее место.

   **_параметры_**:
    - **workspace_id (int):**
      Уникальный идентификатор рабочей области, в которой находится группа.
    - **group_id (int):**
      Уникальный идентификатор группы, которой будет отправлено сообщение.
    - **last_event_id (int):**
      Идентификатор последнего события

9. **join_user_by_invite_link**. Этот метод предназначен для присоединения пользователя к рабочему пространству с помощью ссылки-приглашения.

   **_параметры_**:
    - **link (str):**
      Ссылка на приглашение в рабочее пространство

#### Свойства

- **event_id**: Возвращает идентификатор события.
- **event_type**: Возвращает тип события в нижнем регистре.
- **sender_id**: Возвращает идентификатор отправителя.
- **is_ping**: Флаг, указывающий, является ли событие ping
- **is_message**: Флаг, указывающий, является ли событие сообщением

### GroupBotEvent

Класс GroupBotEvent является производным от класса BotEvent и предназначен для обработки событий, связанных с group.

#### Наследование:

- Класс наследует функциональность от родительского класса BotEvent.

#### Свойства:

- **group** Это свойство возвращает группу, содержащую информацию об объекте group или None.
- **отправитель запроса** Информация о пользователе.

### UserBotEvent

Класс UserBotEvent является производным от класса BotEvent и предназначен для обработки событий, связанных с пользователем.

#### Наследование:

- Класс наследует функциональность от родительского класса BotEvent.

#### Свойства:

- **пользователь** Информация о пользователе.

### GroupUsersBotEvent

Класс GroupUsersBotEvent является производным от класса GroupBotEvent и предназначен для обработки событий, связанных с группой и пользователями.

#### Наследование:

Класс наследует функциональность от родительских классов GroupBotEvent.

#### Свойства:

- **users** Информация о пользователях.

### GroupMeAddBotEvent

Класс GroupMeAddBotEvent является производным от класса GroupUserBotEvent и предназначен для обработки событий, связанных с группой, в которой отправителем запроса является бот.

#### Наследование:

Класс наследует функциональность от родительских классов GroupUserBotEvent.

#### Свойства:

- **group_state** .
- **group** .

### Удалить groupbotevent

Класс DeleteGroupBotEvent является производным от класса BotEvent и предназначен для обработки событий, связанных с удалением группы.

#### Наследование:

Класс наследует функциональность от родительских классов BotEvent.

#### Свойства:

- **deleted_by** Времени удаления.
- **group** Удалена информация о группе.

### HistoryClearGroupBotEvent

Класс DeleteGroupBotEvent является производным от класса BotEvent и предназначен для обработки событий, связанных с удалением истории чатов для всех пользователей.

#### Наследование:

Класс наследует функциональность от родительских классов BotEvent.

#### Свойства:

- **group_id** Группа, в которой была удалена история
- **initial_message_id**
- **initial_message_position**

### UserSignedUpToWorkspaceBotEvent

Класс DeleteGroupBotEvent является производным от класса UserBotEvent и предназначен для обработки событий, связанных с добавлением бота в пространство.

#### Наследование:

Класс наследует функциональность от родительских классов UserBotEvent.

### MessageBotEvent

Класс MessageBotEvent является производным от класса BotEvent и предназначен для обработки событий, связанных с сообщениями.

#### Наследование:

Класс наследует функциональность от родительских классов BotEvent.

#### Методы:

1. **reply_text_message**. Этот метод предназначен для отправки текстового сообщения в одну и ту же группу и рабочее пространство как полученное сообщение.

   **_параметры_**:
    - **message (MessageRequest):**
      Объект сообщения, содержащий текст, идентификатор ответа, кнопки, настройки форматирования

2. **reply_text**. Этот метод предназначен для отправки текста в одну и ту же группу и рабочее пространство как полученное сообщение.

   **_параметры_**:
    - **text (str):**
      Текст сообщения

3. **reply_file**. Этот метод предназначен для отправки файла и текста в одну и ту же группу и рабочую область как полученное сообщение.

   **_параметры_**:
    - **file_path (str):**
      Путь к файлу на диске.
    - **text (str):**
      Текст сообщения

4. **reply_file_message**. Этот метод предназначен для отправки файла и текстового сообщения в одну и ту же группу и рабочее пространство как полученное сообщение.

   **_параметры_**:
    - **file (File):**
      Файл, содержит имя файла и содержимое в байтах.
    - **message (MessageRequest):**
      Объект сообщения, содержащий текст, идентификатор ответа, кнопки, параметры форматирования

5. **reply_multimedia**. Этот метод предназначен для отправки мультимедийных данных и текста в одну и ту же группу и рабочее пространство как полученное сообщение.

   **_параметры_**:
    - **images (Optional[List[Image]]):**
      Список изображений, изображение содержит имя файла и содержимое в байтах
    - **videos (Optional[List[Video]]):**
      Список видео, видео содержит имя файла и содержимое в байтах
    - **message (str):**
      Объект сообщения, содержащий текст, идентификатор ответа, кнопки, настройки форматирования

6. **reply_multimedia_message**. Этот метод предназначен для отправки мультимедийных сообщений и сообщений в ту же группу и рабочее пространство, что и полученное сообщение.

   **_параметры_**:
    - **images (Optional[List[Image]]):**
      Список изображений, изображение содержит имя файла и содержимое в байтах
    - **videos (Optional[List[Video]]):**
      Список видео, видео содержит имя файла и содержимое в байтах
    - **message (MessageRequest):**
      Объект сообщения, содержащий текст, идентификатор ответа, кнопки, настройки форматирования

7. **delete_messages_from_current_group**. Этот метод предназначен для удаления сообщений, отправленных в ту же группу и в ту же рабочую область, что и полученное сообщение.

   **_параметры_**:
    - **message_ids (List[int]):**
      Уникальные идентификаторы сообщений, подлежащих удалению.

8. **confirm_event_from_current_group**. Этот метод предназначен для подтверждения события в одной и той же группе и рабочей области как полученное сообщение.

   **_параметры_**:
    - **last_event_id (int):**
      Уникальный идентификатор события, подлежащего подтверждению.

9. **confirm_read**. Этот метод предназначен для подтверждения отправки сообщения в ту же группу и в ту же рабочую область как полученное сообщение.

   **_параметры_**:
    - **last_msg_id (int):**
      Уникальный идентификатор сообщения, которое необходимо подтвердить.

#### Свойства

- **message_text**: Текст полученного сообщения.
- **reply_message_id**: Идентификатор сообщения, на которое нужно ответить
- **has_selected_button**: Флажок, указывающий, содержит ли сообщение информацию о нажатой кнопке.
- **selected_button**: Информация о нажатой кнопке
- **sender_id**: Идентификатор пользователя, отправившего сообщение
- **message_id**: Идентификатор сообщения

### 3. Обработчики

- **Handler**: Базовый класс для всех обработчиков. Тип события является производным от класса BotEvent.
- **EventHandler**: Обработчик событий. Тип события является производным от класса BotEvent.
- **MessageHandler**: обработчик сообщений. Тип события - MessageBotEvent.
- **CommandHandler**: обработчик команд. Тип события - MessageBotEvent.
- **UpdateMessageEventHandler**: Обработчик обновлений сообщений. Тип события - MessageBotEvent.
- **DeleteMessageEventHandler**: Обработчик удаления сообщений. Тип события - MessageBotEvent.
- **ClickButtonEventHandler**: Обработчик нажатий кнопок. Тип события - MessageBotEvent.
- **GroupHistoryClearHandler**: обработчик для очистки истории группы. Тип события - HistoryClearGroupBotEvent.
- **GroupDeleteHandler**: обработчик удаления группы. Тип события - DeleteGroupBotEvent.
- **GroupMeAddHandler**: Обработчик для добавления в группу. Тип события - GroupMeAddBotEvent.
- **GroupUserRemoveHandler**: обработчик для удаления пользователя из группы. Тип события - GroupUserBotEvent.
- **GroupMeRemoveHandler**: обработчик удаления из группы. Тип события - GroupUserBotEvent.
- **GroupUsersAddHandler**: Обработчик для добавления пользователей в группу. Тип события - GroupUserBotEvent.
- **GroupUserJoinHandler**: Обработчик для пользователя, присоединяющегося к группе. Тип события - GroupUserBotEvent.
- **GroupUserLeaveHandler**: Обработчик для пользователя, покидающего группу. Тип события - GroupUserBotEvent.
- **UserSignedUpToWorkspaceHandler**: обработчик для регистрации пользователя в рабочей области. Тип события
  UserSignedUpToWorkspaceBotEvent.

#### Методы:

1. **should_handle**. Проверяет, может ли этот обработчик обработать это событие.

   **_Parameters_**:
    - **e (T is derived from the BotEvent class):**
      Событие для обработки.

2. **handle**. Обрабатывает событие.

   **_Parameters_**:
    - **e (T is derived from the BotEvent class):**
      Событие для обработки.

## Форматирование текста

### TextMarkups

Класс для работы с текстовой разметкой.

#### Атрибуты класса:

1. **bold (List[TextMarkup])**: Выделение текста жирным шрифтом.
2. **italic (List[TextMarkup])**: Выделение текста курсивом.
3. **underline (List[TextMarkup])**: Выделение текста подчеркиванием.
4. **strike (List[TextMarkup])**: Зачеркнуть текстовую разметку.
5. **monospace (List[TextMarkup])**: Моноширинная текстовая разметка.
6. **url (List[TextMarkup])**: Текстовая разметка Url.
7. **formatted_url (List[TextMarkup])**: текстовая разметка Url.
8. **command (List[TextMarkup])**: Команда разметки текста.

### TextMarkup

Параметры разметки текста.

#### Методы:

1. **__init__**
   Конструктор класса, который инициализирует атрибуты offset, length.

   **_Parameters_**:
    - **offset (int)**: Индекс первого символа в сообщении, с которого начинается текстовая разметка.
    - **длина (int)**: Длина текста, который является разметкой.

2. **convert_to_dict**. Метод, который преобразует объект TextMarkup в словарь.

### TextUrlMarkup

Параметры разметки url.

#### Методы:

1. **__init__**
   Конструктор класса, который инициализирует атрибуты offset, length, url.

   **_Parameters_**:
    - **offset (int)**: Индекс первого символа в сообщении, с которого начинается текстовая разметка.
    - **length (int)**: Длина текста, который является разметкой.
    - **url (str)**: Url-адрес.

2. **convert_to_dict**. Метод, который преобразует объект TextMarkup в словарь.

### FormattingSettings

Параметры форматирования.

1. **__init__**
   Конструктор класса, который инициализирует сообщение, атрибуты text_markups.

   **_Parameters_**:
    - **message (str)**: Текст, содержащий разметку.
    - **text_markups (TextMarkups)**: Данные для текстовой разметки. (опционально)

## Классы для работы с кнопками

### Button

1. **__init__**
   Конструктор класса, который инициализирует сообщение, атрибуты text_markups.

   **_Parameters_**:
    - **id (int)**: Идентификатор кнопки.
    - **label (str)**: текст, отображаемый на кнопке.
    - **callback_message (str)**: Текст, который пользователь увидит в сообщении после нажатия кнопки. (опционально)
    - **callback_data (str)**: Данные, которые получит бот после нажатия кнопки, не отображаются в сообщении. (опционально)

### Выбранная кнопка

Конструктор класса, который инициализирует идентификатор, атрибуты callback_data.

**_Parameters_**:

- **id (int)**: идентификатор нажатой кнопки.
- **callback_data (str)**: Данные из поля Button.callback_data передаются во время инициализации кнопки. (опционально)

## Классы для обработки различных типов контента

### MessageRequest

Класс MessageRequest - это структура данных, предназначенная для хранения информации о текстовом сообщении, которое может быть отправлено группе.

#### Атрибуты класса

1. **text (str)**: Основной текст собщения. Это поле является обязательным и должно содержать содержимое, которое будет отправлено группе.

2. **reply_id (int или None)**: Идентификатор запроса, на который вы хотите ответить.

3. **buttons (List[Button] or None)** Список кнопок, которые можно прикрепить к сообщению.
   Каждая кнопка может обозначать действие, которое может выполнить пользователь (например, "Да", "Нет", "Подробнее").
   Формат каждой кнопки может быть определен отдельно.

4. **formatting_settings (FormattingSettings or None)**: Поле с параметрированием для выбора (для
   например, жирный шрифт, курсив и т.д.). Это позволяет вам более гибко управлять отображением текста.

#### Методы:

1. **__init__**
   Редактор класса, который анализирует текст, идентификатор ответа, кнопки и настройки форматирования.
   Значение для пользователя reply_id, перевод и параметризация могут привести к тому, что пользователь не сможет указать
   все параметры соблюдены.

   **_параметры_**:
    - **text (str)**: основной текст сообщения.
    - **reply_id (int или нет)**: Идентификатор запроса, на который вы хотите ответить.
    - **buttons (List[Button] or None)** Список кнопок, которые можно прикрепить к сообщению.
    - **formatting_settings (FormattingSettings or None)**: Параметрирование выбора.

### Изображение

#### Методы

1. **__init__**
   Конструктор класса, который инициализирует имя, атрибуты содержимого.

   **_параметры_**:
    - **name (str)**: Название изменения.
    - **content (bytes)**: Содержимое изображения в байтах.

2. **upload**.
   Загружает изображение в Память.

   **_параметры_**:
    - **upload_url (str)**: URL-адрес сервера для загрузки изображений в хранилище.
    - **headers (Dict)**: Заголовки.

### Файл

#### Методы

1. **__init__**
   Конструктор класса, который инициализирует имя, атрибуты содержимого.

   **_параметры_**:
    - **name (str)**: Имяхайла.
    - **content (bytes)**: содержание, которое содержится в комментариях.

2. **upload**.
   Загружает файл в хранилище.

   **_параметры_**:
    - **upload_url (str)**: URL-адрес сервера для загрузки файлов.
    - **headers (Dict)**: Заголовки.
    -

### Видео

#### Методы

1. **__init__**
   Конструктор класса, который инициализирует имя, атрибуты содержимого.

   **_параметры_**:
    - **name (str)**: Название вида.
    - **content (bytes)**: Просмотр в байте.

2. **upload**.
   Загружает видео в хранилище.

   **_параметры_**:
    - **upload_url (str)**: URL-адрес сервера для загрузки видео в редакцию.
    - **upload_thumbnail_url (str)**: URL-адрес сервера для загрузки пользовательского содержимого в хранилище.
    - **headers (Dict)**: Заголовки.

## Исключения, связанные с ошибками

### Исключение InvalidInviteLinkException

Ошибка, возникающая, если пригласительная ссылка недействительна.

### Исключение FileUploadException

Ошибка возникает, если загрузка файла была неудачной.

## Example Usage

``` python
import logging
import sys

import requests
from requests import get

from messenger_bot_api import *

root = logging.getLogger()
root.setLevel(logging.DEBUG)

handler = logging.StreamHandler(sys.stdout)
handler.setLevel(logging.DEBUG)
formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')
handler.setFormatter(formatter)
root.addHandler(handler)

token = 'BOT-a25a4807-6b50-445b-adf6-883e0fa111dc-f2917702-8ca5-4427-b923-3cac6464fd5d-1722230974460-c60fd54a-50a5-4fcb-9939-a29e366027cd'
rest = 'https://api-stage2.tty.chat'
sse = 'https://pusher-stage2.tty.chat'
file = 'https://fileupload-stage2.tty.chat'

logger = logging.getLogger('bot')

image_url = 'https://i.guim.co.uk/img/media/26392d05302e02f7bf4eb143bb84c8097d09144b/446_167_3683_2210/master/3683.jpg?width=1900&dpr=2&s=none'
image_response = requests.get(image_url)

video_url = 'https://media.istockphoto.com/id/1388629426/video/top-down-aerial-view-of-mangrove-forest-drone-zoom-out-above-the-tree.mp4?s=mp4-640x640-is&k=20&c=DOH2kXWpimBAFdc1SrUpaksT_TwA3ISDvfzMZNvqIjo='
video_response = requests.get(video_url)


def start_handler(event):
    event.reply_text(
        'Bot commands \n'
        ' /start - start command \n'
        ' /get_bot_ip - get bot ip \n'
        ' /buttons - get message with buttons \n'
        ' /send_multimedia - send multimedia \n'
        ' /send_image - send image \n'
        ' /send_video - send video \n'
        ' /send_file - send file\n'
        'If the message begins with "reply" the bot will send the bot forwarded this message back'
    )


def message_with_buttons_handler(event: MessageBotEvent):
    message = MessageRequest('Message with buttons:', None,
                             [InlineMessageButton(1, "button1", "You pressed button 1", "callbackData"),
                              InlineMessageButton(2, "button2", "You pressed button 2",
                                                  "{'botCommand':{'offset':16,'length':6}}"),
                              InlineMessageButton(3, "button3", "You pressed button 3"),
                              InlineMessageButton(4, "button4", "You pressed button 4")])
    event.reply_text_message(message)


def multimedia_handler(event: MessageBotEvent):
    event.reply_multimedia_message([Image('3683.jpg', image_response.content)],
                                   [Video('3683.mp4', video_response.content)],
                                   MessageRequest('Multimedia'))


def image_handler(event: MessageBotEvent):
    event.send_image_message(event.workspace_id,
                             event.group_id,
                             Image('3683.jpg', image_response.content),
                             MessageRequest('Image'))


def video_handler(event: MessageBotEvent):
    event.send_video_message(event.workspace_id,
                             event.group_id,
                             Video('3683.mp4', video_response.content),
                             MessageRequest('Video'))


def file_handler(event: MessageBotEvent):
    event.reply_file_message(File('3683.jpg', image_response.content), MessageRequest('File'))


def ip_handler(event: MessageBotEvent):
    ip = get('https://api.ipify.org').content.decode('utf8')
    event.reply_text(f'Bot ip is {ip}')


def update_event_handler(event: MessageUpdateBotEvent):
    event.confirm_event_from_current_group(event.event_id)

    if event.comment is not None:
        if event.comment.amount == 1:
            virtual_group_bot = Application(token, {'api_base_url': rest,
                                                    'sse_base_url': 'https://pusher-stage2.tty.chat',
                                                    'group_id': event.comment.virtual_group_id,
                                                    'workspace_id': event.workspace_id,
                                                    'file_upload_base_url': file})
            virtual_group_bot.add_handler(MessageHandler(virtual_group_message_handler))
            virtual_group_bot.start()


def virtual_group_message_handler(event: MessageBotEvent):
    logger.info('virtual_group_message_handler')
    message_handler(event)


def delete_event_handler(event: MessageDeleteBotEvent):
    event.confirm_event_from_current_group(event.event_id)


def messages_delete_event_handler(event: MessagesDeleteBotEvent):
    event.confirm_event_from_current_group(event.event_id)


def group_me_add_event_handler(event: GroupMeAddBotEvent):
    event.confirm_event(event.workspace_id, event.group_id, event.event_id)


def group_me_remove_event_handler(event: GroupMeRemoveBotEvent):
    event.confirm_event(event.workspace_id, event.group_id, event.event_id)


def group_history_clear_event_handler(event: HistoryClearGroupBotEvent):
    event.confirm_event(event.workspace_id, event.group_id, event.event_id)


def group_delete_event_handler(event: DeleteGroupBotEvent):
    event.confirm_event(event.workspace_id, event.group_id, event.event_id)


def group_user_remove_event_handler(event: GroupUserRemoveBotEvent):
    event.confirm_event(event.workspace_id, event.group_id, event.event_id)


def group_user_add_event_handler(event: GroupUsersAddBotEvent):
    event.confirm_event(event.workspace_id, event.group_id, event.event_id)


def group_user_join_event_handler(event: GroupUserJoinBotEvent):
    event.confirm_event(event.workspace_id, event.group_id, event.event_id)


def group_user_leave_event_handler(event: GroupUserLeaveBotEvent):
    event.confirm_event(event.workspace_id, event.group_id, event.event_id)


def user_signed_up_to_workspace_handler(event: UserSignedUpToWorkspaceBotEvent):
    event.confirm_event(event.workspace_id, event.group_id, event.event_id)


def click_button_event_handler(event: MessageBotEvent):
    selected_button = event.selected_button
    message_text = generate_text(selected_button.id, selected_button.callback_data)
    if message_text is not None:
        event.reply_text(message_text)


def generate_text(button_id: int, value):
    if button_id == 1:
        return f'Confirm click by button with id: {button_id} and callbackData {value}'
    if button_id == 2:
        return f'Confirm click by button with id: {button_id} and callbackData {value}'
    else:
        return None


def get_ip_rest():
    try:
        return get('https://api.ipify.org').content.decode('utf8')
    except Exception as e:
        logger.exception('get ip failed', exc_info=e)
        return None


def message_handler(event: MessageBotEvent):
    txt: str = event.message_text.lower() if event.message_text is not None else None
    reply_message_id = None
    if txt == 'hello':
        resp = 'Hi, I am bot'
    elif txt == 'how are you':
        resp = 'I am fine'
    elif txt == 'bye':
        resp = 'Bye'
    elif txt is not None and txt.startswith('reply'):
        resp = 'I replied to the message'
        reply_message_id = event.message_id
    else:
        resp = event.message_text
    event.reply_text_message(MessageRequest(resp, reply_message_id))


def main():
    bot = Application(token, {'api_base_url': rest, 'sse_base_url': sse, 'file_upload_base_url': file})
    bot.add_handler(ClickButtonEventHandler(click_button_event_handler))
    bot.add_handler(CommandHandler('start', start_handler))
    bot.add_handler(CommandHandler('buttons', message_with_buttons_handler))
    bot.add_handler(CommandHandler('send_multimedia', multimedia_handler))
    bot.add_handler(CommandHandler('send_image', image_handler))
    bot.add_handler(CommandHandler('send_video', video_handler))
    bot.add_handler(CommandHandler('send_file', file_handler))
    bot.add_handler(CommandHandler('get_bot_ip', ip_handler))
    bot.add_handler(MessageHandler(message_handler))
    bot.add_handler(UpdateMessageEventHandler(update_event_handler))
    bot.add_handler(DeleteMessageEventHandler(delete_event_handler))
    bot.add_handler(DeleteMessagesEventHandler(messages_delete_event_handler))
    bot.add_handler(GroupMeAddHandler(group_me_add_event_handler))
    bot.add_handler(GroupMeRemoveHandler(group_me_remove_event_handler))
    bot.add_handler(GroupHistoryClearHandler(group_history_clear_event_handler))
    bot.add_handler(GroupDeleteHandler(group_delete_event_handler))
    bot.add_handler(GroupUserRemoveHandler(group_user_remove_event_handler))
    bot.add_handler(GroupUsersAddHandler(group_user_add_event_handler))
    bot.add_handler(GroupUserJoinHandler(group_user_join_event_handler))
    bot.add_handler(GroupUserLeaveHandler(group_user_leave_event_handler))
    bot.add_handler(UserSignedUpToWorkspaceHandler(user_signed_up_to_workspace_handler))

    bot.start()


if __name__ == '__main__':
    main()

```

